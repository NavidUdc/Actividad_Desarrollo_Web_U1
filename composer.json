{
    "name": "crud-usuarios/app",
    "description": "CRUDL de usuarios",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "classmap": [
            "src/"
        ]
    },
    "config": {
        "optimize-autoloader": true
    }
}